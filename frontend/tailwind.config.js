/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      // Theme tokens will be added in Phase 2 per the Frontend Specification Document
    },
  },
  plugins: [],
};
