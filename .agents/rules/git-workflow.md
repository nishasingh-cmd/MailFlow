# Git Workflow Rules

## Strict Commit & Push Policy
- **NEVER run `git commit` automatically under any circumstance.**
- **NEVER run `git push` automatically under any circumstance.**
- Do NOT run `git commit`, `git add`, or `git push` unless the user **explicitly** gives a direct command to do so.
- Keep all modifications strictly in the local working tree without committing.
